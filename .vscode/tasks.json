{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Claude: Start Remote Control",
      "type": "shell",
      "command": "DEVCONTAINER_NAME=$(sed 's|//.*||g' .devcontainer/devcontainer.json | jq -r '.name') && echo \"Starting Claude remote control as: $DEVCONTAINER_NAME\" && claude remote-control --name \"$DEVCONTAINER_NAME\"",
      "presentation": {
        "reveal": "always",
        "panel": "new",
        "focus": true
      },
      "problemMatcher": [],
      "runOptions": {
        "runOn": "folderOpen"
      }
    }
  ]
}