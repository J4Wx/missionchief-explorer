{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dispatch-atlas/schemas/region.schema.json",
  "title": "Region FeatureCollection",
  "description": "A GeoJSON FeatureCollection of facilities for one region.",
  "type": "object",
  "additionalProperties": false,
  "required": ["type", "metadata", "features"],
  "properties": {
    "type": { "const": "FeatureCollection" },
    "metadata": {
      "type": "object",
      "additionalProperties": false,
      "required": ["region_id", "name", "country", "center", "zoom", "schema_version"],
      "properties": {
        "region_id": {
          "type": "string",
          "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
          "description": "Globally unique region slug, e.g. us-ga-savannah."
        },
        "name": { "type": "string" },
        "country": {
          "type": "string",
          "pattern": "^[A-Z]{2}$",
          "description": "ISO 3166-1 alpha-2 of the region itself, e.g. US, GB (not UK). Must match the region_id prefix."
        },
        "game_edition": {
          "type": "string",
          "pattern": "^[A-Z]{2}$",
          "description": "ISO 3166-1 alpha-2 of the Mission Chief edition this region's `game` blocks are written against (US = missionchief.com, GB = missionchief.co.uk). Usually the same as `country`. See docs/02-domain-model.md § Editions."
        },
        "center": {
          "type": "array",
          "description": "[lng, lat] default map center.",
          "minItems": 2,
          "maxItems": 2,
          "prefixItems": [
            { "type": "number", "minimum": -180, "maximum": 180 },
            { "type": "number", "minimum": -90, "maximum": 90 }
          ]
        },
        "zoom": { "type": "number", "minimum": 0, "maximum": 22 },
        "subregions": {
          "type": "array",
          "description": "Optional local-level divisions within the region (e.g. NYC boroughs, county municipalities, city districts). Facilities reference these via properties.subregion_id. May be nested via 'parent' to form a hierarchy.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["id", "name"],
            "properties": {
              "id": {
                "type": "string",
                "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
                "description": "Unique-within-region subregion slug, e.g. manhattan."
              },
              "name": { "type": "string", "minLength": 1 },
              "level": {
                "type": "string",
                "enum": ["borough", "county", "unitary_authority", "municipality", "district", "township", "commune", "ward", "precinct", "sector", "region", "province", "neighborhood", "other"],
                "description": "Kind of local division, for labeling/grouping in the UI. `township`/`precinct` are US divisions; `ward`/`unitary_authority` UK; `commune`/`province`/`region` cover most of Europe."
              },
              "parent": {
                "type": ["string", "null"],
                "description": "Optional id of a parent subregion (for nesting, e.g. neighborhood within borough). Must reference another subregion id."
              },
              "center": {
                "type": "array",
                "description": "Optional [lng, lat] to recenter the map when this subregion is selected.",
                "minItems": 2,
                "maxItems": 2,
                "prefixItems": [
                  { "type": "number", "minimum": -180, "maximum": 180 },
                  { "type": "number", "minimum": -90, "maximum": 90 }
                ]
              },
              "zoom": { "type": "number", "minimum": 0, "maximum": 22 },
              "bbox": {
                "type": "array",
                "description": "Optional [west, south, east, north] bounds.",
                "minItems": 4,
                "maxItems": 4,
                "items": { "type": "number" }
              }
            }
          }
        },
        "generated_by": { "type": "string", "enum": ["agent", "human"] },
        "generated_at": { "type": "string", "format": "date" },
        "last_reviewed": {
          "type": "string",
          "format": "date",
          "description": "ISO date a pass over the whole region last happened — first publication, or a depth pass since (docs/06 § Depth passes). Unlike generated_at it moves; unlike a facility's last_verified it describes the region rather than one record. Mirrored onto the registry entry so review age can be ranked without downloading the file."
        },
        "coverage": {
          "type": "object",
          "additionalProperties": false,
          "description": "What a pass over this region actually covered, so \"nobody looked here\" can be told apart from \"nothing is there\". Optional: an absent block means no one has declared coverage yet, which is a reporting signal rather than an error.",
          "properties": {
            "searched": {
              "type": "array",
              "description": "Categories deliberately searched for. A category listed here with no facilities is a real absence; one missing from this list was never looked for.",
              "items": { "$ref": "facility.schema.json#/properties/category" }
            },
            "gaps": {
              "type": "array",
              "description": "Known holes — things the region has that this file does not carry, stated rather than left to look like absence.",
              "items": {
                "type": "object",
                "additionalProperties": false,
                "required": ["what", "reason"],
                "properties": {
                  "what": {
                    "type": "string",
                    "minLength": 1,
                    "description": "What is missing, in a player's words: \"tow operators\", \"volunteer apparatus rosters\"."
                  },
                  "categories": {
                    "type": "array",
                    "description": "Categories the gap falls under, where it maps onto them.",
                    "items": { "$ref": "facility.schema.json#/properties/category" }
                  },
                  "reason": {
                    "type": "string",
                    "minLength": 1,
                    "description": "Why it is missing — no public addresses, rosters not published centrally, the only aggregator blocks automated access."
                  },
                  "count": {
                    "type": "integer",
                    "minimum": 0,
                    "description": "How many records are known to be missing, where that number is known."
                  }
                }
              }
            },
            "note": { "type": "string" }
          }
        },
        "schema_version": { "type": "integer", "minimum": 1 }
      }
    },
    "features": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["type", "geometry", "properties"],
        "properties": {
          "type": { "const": "Feature" },
          "geometry": {
            "type": "object",
            "additionalProperties": false,
            "required": ["type", "coordinates"],
            "properties": {
              "type": { "const": "Point" },
              "coordinates": {
                "type": "array",
                "minItems": 2,
                "maxItems": 2,
                "prefixItems": [
                  { "type": "number", "minimum": -180, "maximum": 180 },
                  { "type": "number", "minimum": -90, "maximum": 90 }
                ]
              }
            }
          },
          "properties": { "$ref": "facility.schema.json" }
        }
      }
    }
  }
}
